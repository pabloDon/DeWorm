<%= form_for @user_session do |form| %>  
	
	<% if @user_session.errors.any? %>

    <div id="error_explanation">
      	<h2><%= pluralize(@user_session.errors.count, "error") %> prohibited this bug from being saved:</h2>

      	<ul>
      		<% @user_session.errors.full_messages.each do |message| %>
        		<li><%= message %></li>
      		<% end %>
      	</ul>
    </div>

  	<% end %>

	<div class="big-center">  
	    <%= form.text_field :login, :placeholder => "Login" %>  
	</div>
	
	<div class="big-center">  
	    <%= form.password_field :password, :placeholder => "Password" %>  
	</div>
	
	<div class="big-action-center">   
	   <%= form.submit "Submit" %>  
	</div>
	  
<% end %>